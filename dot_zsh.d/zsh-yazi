# y: open yazi and cd to the directory you navigate to on exit
function y() {
    local tmp cwd
    tmp=$(mktemp)
    yazi --cwd-file "$tmp" "$@"
    cwd=$(cat "$tmp")
    [[ -n "$cwd" && "$cwd" != "$PWD" ]] && cd "$cwd"
    rm -f "$tmp"
}
